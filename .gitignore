# Dependencies — downloaded code, reproducible from package.json. Never commit.
/node_modules

# Build output — generated by the framework, not source code.
/.next
/out
/build

# Environment files — may hold secrets (API keys, passwords).
.env
.env*.local

# Logs and OS clutter
*.log
.DS_Store
